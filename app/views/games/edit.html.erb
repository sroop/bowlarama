<%= simple_form_for @game do |f| %>
  <%= f.input :title %>
  <%= f.association :users,
      collection: User.all,
      label_method: :username,
      value_method: :id,
      label: "Opponent" %>
  <%= f.submit %>
<% end %>